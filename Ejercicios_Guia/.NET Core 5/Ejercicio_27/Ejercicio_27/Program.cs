using System;
using System.Collections;
using System.Collections.Generic;


namespace Ejercicio_27
{
    class Program
    {
        static void Main(string[] args)
        {
            
            #region List
            /*
            List<int> numeroIngresado = new List<int>();
            int auxNum;
            Random random = new Random();
            for (int i = 0; i < 20; i++)
            {
                auxNum = random.Next(-20, 10);
                if (auxNum != 0)
                {
                    numeroIngresado.Add(auxNum);

                }
            }

            foreach (int item in numeroIngresado)
            {
                Console.WriteLine("Lista original: {0}", item);
            }

            Console.WriteLine("\n-----------------------------\n");
            numeroIngresado.Sort();
            numeroIngresado.Reverse();

            foreach (int item in numeroIngresado)
            {
                if (item > -1)
                {
                    Console.WriteLine("Lista positivo, decreciente: {0}", item);
                }
            }

            Console.WriteLine("\n-----------------------------\n");
            numeroIngresado.Sort();

            foreach (int item in numeroIngresado)
            {
                if (item < 0)
                {
                    Console.WriteLine("Lista negativos, creciente: {0}", item);
                }
            }
            */
            #endregion





            Console.ReadKey();
        }

        
    }
}
